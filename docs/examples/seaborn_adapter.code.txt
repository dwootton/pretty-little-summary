import pandas as pd
import seaborn

df = pd.DataFrame({"x": [1, 2, 3], "y": [3, 4, 5], "group": ["a", "a", "b"]})
obj = seaborn.FacetGrid(df, col="group")

import pretty_little_summary as pls
result = pls.describe(obj)
print(result.content)
